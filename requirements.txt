# Core FastAPI dependencies
fastapi>=0.110,<0.115
uvicorn[standard]>=0.29,<0.32
python-multipart>=0.0.6  # Required for file uploads (UploadFile, File)

# Database
pyodbc

# Data validation and settings
pydantic>=2.6,<3.0
pydantic-settings>=2.3,<2.5
email-validator>=2.0.0  # Required for EmailStr validation

# Supabase client
supabase>=2.4,<3.0

# HTTP client
httpx>=0.26,<0.28

# Environment variables
python-dotenv>=1.0,<2.0

# Testing (optional, for development)
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<0.25

# Resume Intake API dependencies
openai>=1.0.0
qdrant-client>=1.7.0
pypdf2>=3.0.0
python-docx>=1.0.0
slowapi>=0.1.9
aiocache>=0.12.2
aiofiles>=23.2.1

# Get Recommendations API dependencies
redis>=5.0.1  # Optional: for distributed caching
tenacity>=8.2.3  # Retry logic with exponential backoff

# Outreach Agent V1 API dependencies
sendgrid>=6.11.0  # Email notifications via SendGrid
twilio>=9.3.0  # SMS notifications via Twilio

cryptography